{
  "name": "podfetcher-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Podfetcher backend SDK, CLI, and MCP server",
  "bin": {
    "podfetcher": "./src/cli.js",
    "podfetcher-mcp": "./src/mcp.js"
  },
  "scripts": {
    "lint": "node --check src/*.js",
    "typecheck": "node --check src/*.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
